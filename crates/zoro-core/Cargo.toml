[package]
name = "zoro-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "zoro knowledge base core: parse, index, query, render"

[dependencies]
pulldown-cmark = "0.13"
syntect = { version = "5", default-features = false, features = ["default-fancy"] }
