# agents.md — zoro 开发规范与知识库使用说明

> 本文是每轮开工**必须遵守**的方法论实例（约百行）。反复查阅的细节只留指针，正文在 `kb/`。
> 版本：第二版（2026-09-08，按 kb.md 方案重组；设计结论延续第一版，未扩写完成项）。

## 1. 项目概述

zoro = 本地优先、可扩展的**通用知识库框架**：

- `core`：Go package，负责内容模型 / analyze / manifest / 查询 / 渲染，无 UI / 无 fzf / 无网络。
- 前端：`cmd/zoro`（CLI）、`ext/zoro-launcher`（Wails 桌面）、规划中 `ext/zoro-server`（Web）。
- 内容唯一源：Markdown + `@` 标签块；运行三级流程「内容 → 元数据 → 消费（读元数据 + 按需截原文）」。
- 完整模型与边界：`kb/facts/architecture.md`；为什么这么设计：`kb/facts/decisions.md`。

## 2. 知识库地图

```
kb/
├── INDEX.md             # 索引入口：按“任务场景”路由到 facts/tools/skills
├── facts/
│   ├── architecture.md  # 是什么：模块职责、内容模型、调用链、对外契约
│   ├── decisions.md     # 为什么：选择、否决、代价、何时重新考虑
│   ├── pitfalls.md      # 已知坑：触发→症状→根因→修法，带 active/fixed-in
│   └── glossary.md      # 术语表
├── tools/               # 怎么做（原子配方）：构建/CLI/发布/验收/网关
├── skills/              # 怎么做（端到端 SOP）
└── journal/             # 时序事实（有观察时才创建，不预建空目录）
```

- 接任务先读本文件，再读 `kb/INDEX.md`，按任务只加载相关文件。
- `todos.md` 只记“下一步做什么”；`README.md` 给用户命令速览；设计细节不再堆在根文件。

## 3. 工作流

### 3.1 接任务

1. 先读本文件（每轮必读）。
2. 打开 `kb/INDEX.md`，按任务场景路由到 `facts/`、`tools/` 或 `skills/`。
3. 涉及阶段规划时读 `todos.md`；勿在本文件或 kb 里复制任务状态。

### 3.2 观察与固化（两级协议）

- **L1（自主追加）**：开发中出现的碎片观察写 `kb/journal/`。条目 = `唯一 ID + 日期 + 来源任务 + 1–3 行总结`，不贴原文。子 agent 不直写 journal，由主 agent 汇总落笔（观察 → 时序 → 知识）。
- **L2（确认后固化）**：同主题未固化条目 ≥2，或单发高价值事实（决策/事故根因），向用户提议固化（给出目标文件与大纲）→ 用户确认 → 写入 `facts/`、`tools/` 或 `skills/`，原 journal 标“已固化”。
- **固化默认轨**：同类观察不足 2 条不写进事实源；**快轨**：关键事实当即归纳，不因阈值延误。
- **老化**：journal 超过 30 天未晋升，选择固化 / 标“不固化” / 删除。
- **读取顺序**：先读 `facts/tools/skills` 成品，再扫 journal 中未固化、未过期的增量。
- 已完成并固化的内容不再重复搬运改述，只做增量维护。

### 3.3 纠错即时改

- index 查表 miss 逼你全库 grep 时，当场补路由。
- 发现坑，按 `facts/pitfalls.md` 的固定格式（触发→症状→根因→修法 + 状态）写入或更新。
- 固化时顺手校验 INDEX 路径与单一事实源。

## 4. 核心约定

### 4.1 禁区（不可违背）

- **core 不进 UI / fzf / 网络**；依赖方向永远 `frontend → core`，绝不反向。
- **Markdown 唯一源**：作者只写 Markdown + `@` 指令；HTML/CSS 是产物与样式层。
- **manifest 是派生物**：`.zoro/` gitignore；禁止在 manifest 塞正文副本 / 环境状态 / 配置。
- **配置与内容分离**：偏好进 `zoro.toml`，内容语义进 `@` 标签。
- **身份三元组**：块身份 = `(库名, path, start)`；库名必须进展示路径 / 候选行 / URL。
- **安全默认拒绝**：外部来源内容默认不执行、sanitize、显式确认。
- **设计先于代码**：改 `kb/facts/architecture.md` 或 `kb/facts/decisions.md` 覆盖的点前，先改文档再动代码。

### 4.2 风格与格式

- **存方法不存结果**：结论附推导方式与日期；久远的重新推导。
- **标注实测**：把日期与可信度写进知识本身。
- **一条一事**：facts/tools/skills 条目原子化，可独立引用、更新、删除。
- **结论前置，推导在后**；负面知识（“不要做 X，因为…”）优先记录。
- **不存机密**：只记环境变量名，不记明文密钥 / token。
- **单一事实源**：同一知识只有一个物理位置，其余处全是指针。
- **过期即退**：被修复/取代的条目标状态、摘出路由。
- **测试轻量**：`go test ./...` + 文本夹具；CLI/Launcher 变更按 `kb/tools/build-test.md` 跑验证。
