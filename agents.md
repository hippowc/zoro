# agents.md — zoro 知识库速查表 · 开发指南（定稿 v2）

> 本文件是给「后续负责迭代开发的 agent / 人」读的**单一事实源**。
> 任何 agent 开始本仓库工作前，先读本文件；改动行为前先改本文件。
>
> 区分两个概念：
> - **运行时**：零 agent、零网络、纯本地 —— 产品红线。
> - **开发期**：欢迎 agent 参与，但必须遵守下文"不改的设计"。

---

## 1. 定位与分层

**zoro**：纯本地、零 agent、零网络的通用知识速查系统，体验 = 一条命令 `zoro` + 一次 fzf 交互。

- **工具与内容分离**：`zoro` 是软件（可安装到 PATH）；知识条目是纯文本数据（可 git 分发、可离线获取）。
- **本仓库 = 纯速查表工具（`zoro` 命令）**，不承载真实速查内容；真实速查表另建独立内容库承接，通过「内容根」配置接入本工具，未来支持多内容根。
- 运行时零 agent、零网络、零持久化数据库；依赖只有 Unix 工具链 + fzf。

## 2. 三条原则（不可违背）

1. **目录自由，内容受限**：内容条目目录零约束；唯一技术约束在条目「标记行」的格式上（§3）。
2. **两个技术要素，均以单行标记承载**：
   - `tags`：检索范围过滤器，启动时指定，多 tag 为 OR；空 = 不限定。
   - `index`：检索命中面，fzf 唯一匹配文本。手写为主，允许离线辅助生成。
3. **唯一强制物是索引**：一个扁平 TSV，五列 —— `title  tags  index  start  path`（title 为 v2 定稿新增，见 §4）。

## 3. 条目格式（唯一内容契约）

每个条目 = 可读标题（可选）+ 一行标记 + 自由正文：

```markdown
## 定投策略（可读性标题，可选）

::zoro tags=invest,纪律 index=定投 止盈 资产配置

内容主体：代码 / 命令 / 表格 / 步骤，任意形式，
可以多行，可以含代码块。
```

### 3.1 标记行（定稿）

- 形如 `::zoro tags=逗号分隔的tag集合 index=空格分隔的关键词`。
- `tags` 可为空（`::zoro index=...`）；`index` 必须存在（可为多个词）。
- **起点 = `::zoro ` 行**，`start` 即该行行号（1-based）。
- 扫描正则：`rg -n '^::zoro '`。
- 标记可单点替换：若未来需要"渲染时完全隐藏"，可整体换成 `<!-- zoro: tags=... index=... -->`，其余设计不变。

### 3.2 边界规则（现场推导，不存 end）

- **条目边界 = 该 `::zoro ` 行 → 下一个 `::zoro ` / 下一个 `##` / 文件末尾**。
- 展示时按 `path` + `start` 现场截取，不记录 `end`。
- `::zoro` 开关词是保留字：正文中刻意避免出现以 `::zoro ` 开头的行（与正文冲突概率≈0，因为任意 Markdown/代码都不生成这种行）。

### 3.3 title 规则（字段语义）

- 构建索引时，从 `start` **向上**找最近的 `##` 行，取作 `title`。
- 一个 `##` 下多条目标记 → 继承同一标题。
- 找不到 `##` → `title` 取 `index` 的首个词兜底。

### 3.4 index 规则

- 空格分隔关键词列表，是 fzf 的「命中文案」。
- **index 是唯一命中面，正文不可检索**（设计红线，见 §5 注）。
- 优先手写精准词；辅助生成（大模型/工具）只发生在**离线一次性阶段**，产物经人确认后写回标记行；**运行时永不调用任何生成器**。

## 4. 索引（唯一强制物）

**五列 TSV，入库随库分发：**

```
title    tags    index    start    path
```

字段约定：

| 列     | 说明 |
|--------|------|
| `title`| 条目可读标题（§3.3）；单行、不得含 `\t`。 |
| `tags` | 逗号分隔 tag 集合；空 = 不限。不得含 `\t`。 |
| `index`| fzf 匹配核心文本；单行、不得含 `\t`。 |
| `start`| `::zoro ` 标记行在源文件内的 1-based 行号。 |
| `path` | 相对内容根的源文件路径。 |

### 4.1 生成与失效策略（v2 定稿：预构建 + 脏检查，不再"每次重建"）

- 默认索引文件：内容根下的 `zoro-index.tsv`（随内容库分发、可提交）。
- **查询前做一次毫秒级脏检查**，满足全部条件则直接用现成索引，不重建：
  - `zoro-index.tsv` 存在；
  - 内容根内**没有**比它更新的 `*.md` / `*.mdx` 文件（`find "$ZORO_ROOT" -name '*.md' -newer zoro-index.tsv -print -quit` 为空）。
- 检测到变更才重建；索引永远是**派生物**、随时可重建，但日常查询零扫描成本。
- 写入索引必须**原子化**：先写临时文件再 `mv` 覆盖，避免脏读。

### 4.2 扫描范围（工具 ≠ 内容）

- 第一版：**单内容根**，由环境变量 `ZORO_ROOT` 指定，不默认指向本仓库任何目录；真实内容来自仓库外的独立内容库。
- 只扫 `*.md` / `*.mdx`；跳过隐藏目录（`.git` 等）。
- 绝不扫工具代码目录（`bin/`、`lib/`、`tests/`）。
- 多内容根为 future work；`path` 以"相对内容根"存储，保证分发后仍可定位。

## 5. 检索链路（`zoro` 命令）

### 5.1 CLI（定稿）

```bash
zoro [-t tag1,tag2] [初始query]
```

- `-t`：tags 过滤，**多个 tag 为 OR**（命中任意一个即保留）。可省略 = 不限定。
- `[初始query]`：进入 fzf 的**预填**，不是过滤；进去后可退格/清空/改词。
- 无参数 `zoro` = 全量进入 fzf。

### 5.2 两段语义（tags 与 index 彻底分工）

1. **tags = 第一道硬过滤**：命令行指定，进入 fzf 前用 `awk` 砍掉不相关条目（OR 关系）。
2. **fzf 里的一切输入 = 只与 `index` 列模糊匹配**：正常使用 fzf 即可，tags 不再参与。

> 设计红线：**「命中面」必须收敛到人写的 `index`，不得发散到正文。** 全文搜索的问题不是搜不到，而是"搜索到后不知展示什么"——行匹配的上下文是熵，index 是结构。谁想加全文搜索，先读这一句。

### 5.3 固定管线（顺序不可改）

1. **脏检查**：§4.1 判定索引是否新鲜，否则重建。
2. **awk 按 tags 预过滤**：给定 `-t` 时，保留 tags 集合与用户 tags 集合交集非空的行；未给定则全保留。
3. **fzf 交互**：候选行结构 + 显示/匹配分离（§5.4），`--preview` 实时预览。
4. **确定展示**：回车后 clear fzf，按 `path` + `start` 截取条目（§3.2），bat 高亮（降级 cat）。

### 5.4 fzf 显示 / 匹配分离（关键）

候选行是**结构行**（tab 分隔，携带全部字段）：

```
title<TAB>tags<TAB>index<TAB>path<TAB>start
```

- `--with-nth=1`：**显示**第 1 列（可读标题）。
- `--nth=3`：**匹配**第 3 列（index）。
- 效果：用户看到标题、敲的是 index，两者分离 —— 候选区因此可以显示标题而不是 index。
- `--delimiter` 用 `\t`。

### 5.5 --preview（必备）

- fzf `--preview-window=right,60%,wrap`（支持滚动）。
- 预览脚本接收候选行，解析出 `path` + `start`，用一条轻量 awk/sed 现场截取条目（毫秒级），bat 高亮（`--paging=never`，否则 preview 里触发分页会卡）。
- 回车确定后，再做一次完整全屏展示（preview 只是预览，最终展示独立执行）。

### 5.6 非 TTY 降级

- 启动时检测 `[ -t 0 ]`：
  - 有 TTY：正常 fzf 交互。
  - 无 TTY（管道 / CI / cron / ssh 无 -t）：退出交互，按相同过滤逻辑**顺序输出所有命中条目正文**到 stdout（等价 grep 式全量输出），保证 `zoro | less` 可用。

## 6. 依赖与降级

| 组件 | 角色       | 降级        |
|------|------------|-------------|
| fzf  | 交互核心   | **无（唯一硬依赖）**；缺失即报错并提示安装 |
| bat  | 高亮展示   | cat         |
| rg   | 扫描/构建  | grep -n     |

- `awk`、`sed`、`find` 属基础工具链，视为必装。
- 非 TTY 下不使用 fzf（§5.6），但 fzf 二进制仍需存在于装有 `zoro` 的机器（交互是主体能力）。

## 7. 目录布局

```
zoro/
├── README.md
├── agents.md              # 本文件（开发事实源）
├── zoro-index.tsv         # 预构建索引（入库、随库分发）
├── bin/
│   └── zoro               # 主命令入口（POSIX sh / bash 可执行）
├── lib/
│   ├── ensure-index.sh    # §4.1 脏检查 + 按需重建
│   ├── build-index.sh     # 扫描 ::zoro 行 → 五列 TSV（含 title 解析）
│   ├── filter-tags.awk    # §5.2 tags 预过滤（OR）
│   └── show-entry.sh      # §3.2 边界推导 + 截取 + 高亮
└── tests/                 # 最小验收夹具（fixtures 条目 .md + 纯 shell 断言）
```

> 本仓库**不放真实速查条目**；测试夹具在 `tests/fixtures/`，真实内容库在仓库外通过 `ZORO_ROOT` 接入。


## 8. 开发路线图（按依赖顺序，逐步验收）

1. **单条目边界手测**：1 个示例条目，awk/sed 证明「从 start 截到下一个 `::zoro `/`##`/EOF」边界正确（含连续两条目、条目到 EOF、`##` 结尾）。
2. **build-index**：`rg -n '^::zoro '` 生成五列 TSV；解析 tags/index/title/path/start；处理 tags 空、index 多词、路径含空格。
3. **ensure-index 脏检查**：mtime 判定 + 原子写 `zoro-index.tsv`。
4. **tags 预过滤**：awk 按 `-t` 过滤，多 tag OR；空 tags 通过全部。
5. **fzf 接线**：结构候选行 + `--with-nth=1` 显示 title + `--nth=3` 匹配 index；回车回查命中行。
6. **--preview + 确定展示**：preview 现场截取；回车后 clear + `show-entry.sh` 全屏展示（bat→cat）。
7. **非 TTY 降级**：`[ -t 0 ]` 分支 + 顺序输出全部命中。
8. **入口 `zoro` 聚合**：解析 `-t` / 初始 query，串起 ensure→filter→fzf→show；错误信息人话化（尤其"请安装 fzf"）。
9. **tests/ 验收夹具**：覆盖边界推导、多 tag OR、空 tags、title 继承/兜底、index 含代码标识符、特殊字符路径、mtime 触发重建。

## 9. 开发约定

- **不改设计**：§2/§3/§4/§5 的任何改动都是设计变更，先改 `agents.md` 再动代码。
- **运行时三零**：查询路径不允许任何 agent 调用、任何网络请求、任何数据库。
- **性能目标**：千级条目内，未变更时查询进入 fzf 延迟应近零（只读现成索引）；变更重建控制在毫秒~百毫秒级。
- **可移植性**：优先 POSIX sh + awk；用 bash 特性时声明 `#!/usr/bin/env bash`。
- **索引是派生物**：随时可重建，但作为分发产物入库；提交前保证它干净（对应内容根已无更新）。
- **测试轻量**：纯 shell + 文本断言，不引入测试框架与第三方依赖。

## 10. 验收标准（定稿）

- [ ] `zoro` 无参进入 fzf 全量浏览；候选区显示标题。
- [ ] `zoro -t a,b` 为 OR 过滤；进入后输入只匹配 index。
- [ ] `zoro 初始词` 预填 query，可清空/改写。
- [ ] 连续两条 `::zoro` 条目各显示正确、无粘连；条目以 `##`/EOF 结尾不越界不丢尾。
- [ ] title 正常解析：有 `##` 用 `##`，多条目继承，无 `##` 用 index 首词。
- [ ] 内容未变：直接读 `zoro-index.tsv`，不重建；某 `.md` 更新后自动重建。
- [ ] 缺 bat / rg 自动降级；缺 fzf 报错并给出安装指引；无 TTY 时输出全部命中且 `zoro | less` 可用。
- [ ] `--preview` 随选随显；回车后全屏展示不卡分页。
- [ ] 从零克隆 → 放置内容 → `zoro` 全链路可用，不依赖旧状态。
