# 示例内容库

## 丢弃本地修改
@index git checkout reset 丢弃 还原
@cmd
```bash
git checkout -- <file>
git reset --hard <commit>
```

正文说明：这是第一条示例条目。

## 资产配置
@index 定投 止盈 资产配置
@cmd
```bash
echo "invest"
```

正文说明：这是第二条示例条目。
