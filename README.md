# zoro

纯本地、零 agent、零网络的**通用知识库速查表工具**。

- 查询入口：`zoro` 命令 + fzf 交互；**单二进制**（Rust），捆绑 fzf，零额外安装
- 内容标记：`@index` 一行标注检索命中面；`@cmd` 借用 Markdown 原生代码块声明可执行命令；其余正文自由写
- 工具与内容分离：本仓库只含工具，真实内容在**外部内容库**，经环境变量 `ZORO_ROOT` 接入
- 设计与开发规范见 [agents.md](agents.md)
