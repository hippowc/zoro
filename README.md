# zoro

本地优先、可扩展的**通用知识库框架**。

- 核心 = `zoro-core`（library：解析 / 索引 / 查询 / 渲染管线）+ `zoro`（最小 CLI 前端，捆绑 fzf）
- 内容源：**Markdown 唯一**；统一标签模型：每个 `@` 标签是一个块（标签名 + 搜索词 + 负载），如 `@index` 叙述、`@shell` 终端执行
- 多 target 展示：终端 / 本地 Web / 桌面 / 静态站点（扩展独立）
- 愿景路线：工具 → 站点 → 云 → 安全
- 设计与开发规范见 [agents.md](agents.md)
