# 多语言示例

## 读取文件
@index rust python 读取文件 示例
@cmd
```python
with open("a.txt") as f:
    print(f.read())
```

## 并发请求
@index rust 并发 请求
@cmd
```rust
let handles: Vec<_> = urls.iter().map(|u| tokio::spawn(fetch(u))).collect();
```
